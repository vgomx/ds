Tooltip — a terse ink bubble on hover/focus, with a small arrow.

```jsx
<Tooltip label="Copy link"><Button variant="secondary">Share</Button></Tooltip>
```

Wrap the trigger as the child. Keep the text short (a few words); for richer content with actions, use a popover/`Card` instead. `side` is `top` or `bottom`.
