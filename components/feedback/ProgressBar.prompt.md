ProgressBar — a thin track with an ink/accent fill. Determinate or indeterminate.

```jsx
<ProgressBar value={68} label="Upload" />
<ProgressBar indeterminate label="Working" />
```

6px track, 2px radius. Pass `value` (0–100) for a determinate bar, or `indeterminate` for an unknown-duration sweep.
