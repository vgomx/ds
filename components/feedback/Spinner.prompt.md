Spinner — a hairline ring with an accent leading edge.

```jsx
<Spinner />
<Spinner size="lg" label="Loading…" />
```

Pass a preset (`sm | md | lg`) or a pixel diameter. Add `label` for an inline mono caption.
