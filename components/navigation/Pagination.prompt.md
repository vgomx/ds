Pagination — square mono page cells; the active page fills with the accent. Edges clamp and the middle collapses to an ellipsis.

```jsx
<Pagination total={9} page={page} onChange={setPage} />
```

1-indexed. Prev/next arrows disable at the ends. For paging a work index or blog list.
